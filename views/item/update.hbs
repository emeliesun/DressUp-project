{{>nav}}
{{> side_menu}}
{{> sidemenu_open_btn}}

<div id="top-image">
    <img src="/images/{{item.image}}" alt="" class="outfit-feed-img">
</div>
{{!-- //////////////////////////////////////////////////////////////////////// --}}

<h2 id="createH2">Update Item</h2>
<div id="form-frame">
    <form action="/item/update" method="POST" id="form-frame-inside" enctype="multipart/form-data">

        <input type="text" name="id" value="{{item._id}}" hidden>

        <div class="form-group">
            <label for="item-img">Choose an image</label>
            <input class="form-control" type="file" name="item-img">
        </div>

        <div class="form-group">
            <label for="title">Title:</label>
            <input type="text" name="title" class="form-control" value="{{item.title}}" placeholder="{{item.title}}">
        </div>

        <div class="form-group">
            <label for="material">Material</label>
            <select name="material" required id="item-material" class="form-control">
                <option value="{{outfit.occasion}}">Make a choice</option>
                <option value="Mocha">Mocha</option>
                <option value="Leather">Leather</option>
                <option value="Cotton">Cotton</option>
            </select>
        </div>

        <div class="form-group">
            <label for="brand">Brand</label>
            <select name="brand" required id="item-brand" class="form-control">
                <option value="{{outfit.occasion}}">Make a choice</option>
                <option value="YSL">YSL</option>
                <option value="Gucci">Gucci</option>
                <option value="Louis Vuitton">Louis Vuitton</option>
            </select>
        </div>

        <button type="submit" class="btn btn-primary" formmethod="POST" id="btn-primary-green-login" id="">Update</button>
    </form>
</div>
